-------------------------REVERSE----------------------------------------
from django.core.urlresolvers import reverse

reverse('arch-summary', args=[1945])
reverse('admin:app_list', kwargs={'app_label': 'auth'})
------------------------------------------------------------------------

--------------------------PRE_SAVE--------------------------------------
В хендлер передаётся аргумент raw, если он равен True значит данные заполняются
из фикстур.
------------------------------------------------------------------------

